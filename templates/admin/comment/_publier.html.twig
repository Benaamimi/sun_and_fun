<div class="container">
		{{ form_start(form) }}
		{{ form_row(form.isPublished) }}

		<button type="submit" class="btn btn-success btn-sm">Publier le commentaire</button>
		{{ form_end(form) }}
	</div>