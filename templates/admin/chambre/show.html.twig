{% extends "admin/base.html.twig" %}

{% block title %}
	Page de
	{{ chambre.titre }}
{% endblock %}

{% block body %}
	<div class="container py-5">
		<div class="row">
			<div class="col-lg-8">
				<div class="col-lg-8 col-6 mb-2">
					<img class="img_fluide" src="{{ asset("/uploads/" ~ chambre.image) }}" alt="Image de {{ chambre.titre }}" width="500">
				</div>
				<div class="text-block mt-4">
					<h1>{{ chambre.titre }} - {{ chambre.prixJournalier }}€ /Nuit</h2>
					</h1>
					<p class="text-muted fw-light">{{ chambre.Description }}</p>
				</div>
			</div>
		</div>
		<a class="btn btn-sm btn-info me-2 mt-4" href="{{ path('admin_chambre_edit',{ 'id' : chambre.id }) }}">
			<i class="fas fa-pen me-2"></i>
			Modifier
		</a>
		<a class="btn btn-sm btn-danger mt-4" href="{{ path('admin_chambre_delete', { 'id' : chambre.id }) }}">
			<i class="fas fa-trash me-2"></i>
			Supprimer
		</a>

		{% include "admin/chambre/_disponibilite.html.twig" %}
		
	</div>


{% endblock %}
