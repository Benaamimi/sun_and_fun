{% extends "admin/base.html.twig" %}

{% block title %}Sun and Fun | backOffice
{% endblock %}

{% block body %}
	<div class="container mt-4">

		<h1 class="fw-bold">Bonjour "{{ app.user.nom }}" !</h1>
		<h3 class="fw-light">Vous êtes connecté dans la plateforme de gestion des tâches.</h3>

		<div class="mt-5">

			<div class="mt-4">
				<a href="{{ path('admin_reservation_index') }}" class="text-decoration-none">
					<h5><i class="fa fa-ticket me-2"></i>Gestion des réservations</a></h5>
			</div>

			{# {% if is_granted("ROLE_ADMIN")  %} #}
				<div class="mt-4">
					<a href="{{ path('admin_user_index') }}" class="text-decoration-none" class="text-decoration-none">
						<h5><i class="fa fa-user me-2"></i>Gestion des utilisateurs</a></h5>
				</div>
			{# {% endif %} #}

			<div class="mt-4">
				<a href="{{ path('admin_chambre_index') }}" class="text-decoration-none">
					<h5><i class="fa fa-bed me-2"></i>Gestion des Chambres</a></h5>
			</div>
			<div class="mt-4">
				<a href="{{ path('admin_comment_index') }}" class="text-decoration-none">
					<h5><i class="fa fa-comment me-2"></i>Gestion des Avis</a></h5>
			</div>
			<div class="mt-4">
				<a href="{{ path('admin_contact_index') }}" class="text-decoration-none">
					<h5><i class="fa fa-comment me-2"></i>Gestion des messages</a></h5>
			</div>
		</div>

	</div>


{% endblock %}
