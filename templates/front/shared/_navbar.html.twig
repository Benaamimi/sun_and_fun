<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor04" aria-controls="navbarColor04" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor04">
			<ul class="navbar-nav me-auto ms-4">
				<li class="nav-item">
					<a class="nav-link active" href="#">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Features</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Pricing</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">About</a>
				</li>
			</ul>
			{% if app.user %}
				<div class="btn-group">
					<a type="button" class="btn btn-outline-info btn-sm dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
						<i class="fa fa-user me-2"></i>{{ app.user.email }}
					</a>
					<ul class="dropdown-menu">
					{% if is_granted("ROLE_ADMIN") %}
						<li>
							<a class="dropdown-item" href="{{ path('admin_home_index') }}">Back office</a>
						</li>
					{% endif %}
						<li>
							<a class="dropdown-item" href="#">Ma réservation</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
						</li>
					</ul>
				</div>
			{% else %}
				<a class="btn btn-outline-info btn-sm" href="{{ path('app_login') }}">Connexion</a>
				<a href="{{path('app_inscription')}}" class="btn btn-outline-primary btn-sm ms-2">Inscription</a>
			{% endif %}

			{# {% if not app.user %}
						{% endif %} #}
		</div>
	</div>
</nav>
